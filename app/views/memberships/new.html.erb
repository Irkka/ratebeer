<p><%= notice %></p>
<h2>Join a Club!</h2>
<%= form_for(@membership) do |f| %>
		<%= f.select :beer_club_id, options_from_collection_for_select(@beer_clubs, :id, :name) %>
		<%= f.submit %>
<% end -%>
